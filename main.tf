terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

# Configure the AWS Provider
provider "aws" {
  region = "us-east-1"

  # Profile Access Key e Secret Key configurado em ~/.aws/credentials
  profile = "RoadMap"
}

